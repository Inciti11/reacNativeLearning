import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 20
  },
  input: {
    height: 50,
    borderWidth: 1,
    borderColor: '#CED4DA',
    borderRadius: 8,
    paddingHorizontal: 15,
    fontSize: 16
  }
});